# Test configuration for bitbucket pipelines.
image: python:2.7.11

pipelines:
  default:
    - step:
        script:
          - pip install -e .
          - pip install pytest pytest-mock pytest-catchlog
          - py.test test/unit -vv -k-slow